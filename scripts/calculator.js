/* //MVC significa Model View Controller, Model são os dados, a sua manipulação, 
View é o que você tá vendo, a interface que o usuário está vendo 
o controller são as regras de negócios em o que pode ou não ser feito */
//eu em vez de usar let, usando window.calculator, ela fica disponivel lá na console do browser para testar, ai é só chamar lá calculator
window.calculator = new CalcController();//to dizendo que calculladora é uma nova instancia/cópia/objeto da classe CalController

